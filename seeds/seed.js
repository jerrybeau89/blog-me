const sequelize = require('../config/connection');
const user = require('./user-seeds');
const userPost = require('./post-seeds');
const userComment = require('./comment-seeds');

const seedDatabase = async () => {

};

seedDatabase();